ignores:
  # used by name inside vitest.config.mts / turbo, not imported from source
  - jsdom
  - npm-check-updates
  - "@testing-library/jest-dom"
  # dependencies of the demo app that are not wired up yet
  - fuse.js
  - solid-command-palette
  - solid-transition-group
  - tinykeys
